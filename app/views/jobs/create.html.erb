<h1>hi</h1>
<%= form_for @job do |j| %>
	<%= j.label :name %>
	<%= j.text_field :name %>

	<%= j.label :origin %>
	<%= j.text_field :origin %>

	<%= j.label :destination %>
	<%= j.text_field :destination %>

	<%= j.label :cost %>
	<%= j.text_field :cost %>

	<%= j.label :containers %>
	<%= j.text_field :containers %>

	<%=j.submit %>
<% end %>

<!-- not sure if this form does anything because new.html.erb is used -h -->